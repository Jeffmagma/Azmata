package Game;

import java.awt.*;

/**
 * A 4-way [theres a word for this] direction TODO
 */
public enum Direction {
    DOWN, LEFT, RIGHT, UP;

    Point move(Point start, int amount) {
        switch (this) {
            case DOWN: break;
            case LEFT: break;
            case RIGHT: break;
            case UP: break;
        }
        return null;
    }
}
